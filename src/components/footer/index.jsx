import React from 'react'
import { LIMIT } from '../../constants'

const Footer = () => {
  return (
    <footer>
      <div className="container">Footer {LIMIT}</div>
    </footer>
  )
}

export default Footer